The program is written in python. It uses the following modules. Please make sure you have the following modules installed. You can use pip to install these:
1. yaml (pip install pyYAML)
2. requests (pip install requests)

The program expects the path to the yaml file as an argument.
Here is a sample command:
python health_checker.py links.yaml